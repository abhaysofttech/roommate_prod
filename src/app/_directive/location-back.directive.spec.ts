import { LocationBackDirective } from './location-back.directive';

describe('LocationBackDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationBackDirective();
    expect(directive).toBeTruthy();
  });
});
